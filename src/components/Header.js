import React from "react";
import styles from "./Header.pcss";

const Header = () => {
  return (
    <div className={styles.root}>
      <h1>Arporaattori</h1>
    </div>
  );
};

export default Header;
